# Enforce the Node.js version defined in the "engines" field of package.json
engine-strict=true

# Disable package-lock.json generation
package-lock=false

# Fall back to pre-npm7 peer dependency resolution to avoid install conflicts
legacy-peer-deps=true

# Always save exact versions for reproducible builds
save-exact=true
