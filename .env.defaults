# ==============================================================================
# React Starter Kit - Environment Configuration
# ==============================================================================
# Copy this file to .env and update the values for your environment
#
# IMPORTANT FOR PRODUCTION:
# - DO NOT deploy .env file to production servers
# - Set environment variables directly on the server instead
# - See DEPLOYMENT.md for production environment variable setup
# ==============================================================================

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------

# Port for the Node.js application server
RSK_PORT=1337

# Host for development server (localhost, 0.0.0.0, etc.)
RSK_HOST=localhost

# Enable HTTPS for development server (true/false)
RSK_HTTPS=false

# ------------------------------------------------------------------------------
# API Gateway Configuration
# ------------------------------------------------------------------------------

# Base URL for client-side API requests (browser)
# Leave empty to use relative URLs (e.g., /api/users)
# Set to external URL if API is on different domain (e.g., https://api.example.com)
RSK_API_BASE_URL=

# External API Proxy URL (optional)
# If set, proxies /api/* requests to this external API
# This allows you to hide API keys and avoid CORS issues
# Example: RSK_API_PROXY_URL=https://api.github.com
# Client request: /api/users → proxied to https://api.github.com/users
# Leave empty to disable external API proxying
RSK_API_PROXY_URL=

# ------------------------------------------------------------------------------
# Application Metadata
# ------------------------------------------------------------------------------

# Application name (used in page titles)
RSK_APP_NAME="React Starter Kit"

# Application description (used in meta tags)
RSK_APP_DESCRIPTION="Boilerplate for React.js web applications"

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------

# Database connection URL
# Examples:
#   SQLite: sqlite:database.sqlite
#   PostgreSQL: postgresql://user:password@localhost:5432/dbname
#   MySQL: mysql://user:password@localhost:3306/dbname
RSK_DATABASE_URL=sqlite:database.sqlite

# ------------------------------------------------------------------------------
# Authentication Configuration
# ------------------------------------------------------------------------------

# JWT secret key for token signing
# IMPORTANT: Change this in production to a strong random string
RSK_JWT_SECRET=

# JWT token expiration time
# Examples: 60s, 5m, 2h, 7d
RSK_JWT_EXPIRES_IN=7d

# ------------------------------------------------------------------------------
# Additional RSK_ Variables (Optional)
# ------------------------------------------------------------------------------
# All variables above use the RSK_ prefix for consistency and to indicate they
# are React Starter Kit specific configuration.
#
# You can add additional RSK_ prefixed variables for:
# - Feature flags
# - Build-time configuration
# - Application-specific settings
#
# Examples:
# RSK_FEATURE_NEW_UI=true
# RSK_MAX_UPLOAD_SIZE=10485760
# RSK_ENABLE_ANALYTICS=false
# RSK_APP_VERSION=1.0.0
#
# Note: These values are BAKED INTO THE BUNDLE at build time.
# Changing them requires rebuilding the application.

# ------------------------------------------------------------------------------
# Development Server Configuration (Optional)
# ------------------------------------------------------------------------------

# Open browser automatically on dev server start (true/false)
# Defaults to true unless in CI environment or silent mode
# DEV_OPEN_BROWSER=true

# ------------------------------------------------------------------------------
# Build Configuration (Optional)
# ------------------------------------------------------------------------------

# Enable bundle analysis
# BUNDLE_ANALYZE=false

# Enable build profiling
# BUNDLE_PROFILE=false

# Log level: silent, error, warn, info, verbose, debug
# LOG_LEVEL=info

